AS LEIS VIRARAM TRAVAS, E A BATERIA "SEM REDE" TOCAVA REDE (01/Set/2026)

Auditoria do Claude Code sobre o projeto + 7 passos aprovados um a um.
Nenhuma regra de negócio mudou; mudou o quanto delas é cobrado POR MÁQUINA.

1) DUAS VERDADES NA PRÓPRIA CONFIGURAÇÃO. O .claude/CLAUDE.md dizia
   ARTICLE_ANALYSIS_PROMPT_v2 (aposentado), TTS onyx (é cedar) e raiz
   /Users/edcastro77/ — toda sessão abria lendo isso. Reescrito como
   ponteiro para o CLAUDE.md da raiz + IDs de infra. Marketing: 2
   caminhos mortos corrigidos.

2) HOOKS (.claude/hooks/, 4 scripts + settings.json). LEI 12: cp/mv/rm
   sobre saidas/outputs/ARTIGOS confere origem 0-byte (NEGA — o caso do
   gabarito de 20/Ago) e sobrescrita (PERGUNTA). LEI 5: SQL/REST de
   escrita em `artigos` NEGADO em Bash E no MCP Supabase. Arquivos
   sagrados pedem confirmação; .env ninguém edita. Todo .py editado
   compila na hora. Provados com 25 casos + violação deliberada negada
   ao vivo.

3) A PROVA EM TRÊS CASAS. Skill /prova (portão da Chave 8 sem merge),
   workflow prova.yml a cada push (acusa vermelho, não bloqueia — os
   secrets já existiam), e as skills /leis e /varredura + subagents
   cardiodaily-prova, streamlit-admin e guardiao-leis.

4) A BOMBA DOS 1000 NA CHAVE 3. Medido: 882 linhas no banco, buscar()
   sem limite, e o PostgREST corta em 1000 SEM AVISAR — a 118 artigos
   do painel esconder os de nota baixa em silêncio, com o banner "X de
   Y" contando um Y falso. Varredura LEI 9: era o ÚNICO leitor
   ilimitado de `artigos`. Agora pagina (Range, blocos de 1000,
   desempate doc_id). Trava nova provada contra o código VELHO do git.

5) O ARTIGO INAPROVÁVEL. A suíte nova de AppTest acusou na PRIMEIRA
   rodada: tela 861 × selectbox 860. As partes 1 e 2 de "Mechanical
   Circulatory Support…" (iccl .001/.002, mesma nota/revista/80 chars)
   colapsavam no dict de rótulos — uma ficava INVISÍVEL para a
   curadoria. Rótulo repetido agora ganha o DOI.

6) A TELA TEM PROVA PRÓPRIA. teste_administrador.py (AppTest, 13
   verificações, Chave 26): abre sem exceção, filtros como o dono
   decidiu, e o "Y no banco" conferido contra contagem INDEPENDENTE no
   Supabase (882 = 882).

7) A BATERIA DIZIA "SEM REDE" E FAZIA CHAMADA DE REDE. import
   administrador executava a UI inteira — inclusive o GET no Supabase —
   dentro de teste_quem_grava_e_quem_le (descoberto simulando o CI sem
   .env). A UI virou main(), chamada só com ScriptRunContext; passa()
   ficou 100% parametrizada (a meia-lição de 21/Ago, completada);
   _SO_SEM_TEMA nasce False (NameError latente). PROVADO: ambiente
   VAZIO, sem credencial → 91 travas, APROVADO, exit 0. Primeira vez
   que a bateria é offline DE FATO.

Bateria: 91/91. AppTest: 13/13. Comentários-memória preservados.
Registro: CADERNO PARTE 21 (+ ponteiros nas PARTES 14 e 16).
Pendente do dono: abrir a Chave 3 no navegador uma vez; conferir o
primeiro "Prova do Motor" verde na aba Actions.
