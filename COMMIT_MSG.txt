Auditoria das chaves: o erro fatidico do reuso + 4 buracos operacionais

O #34 (a pasta manda) estava certo e mesmo assim o erro voltou. A causa nao
estava no analisador — estava no laco de blocos:

    if os.path.exists(pasta + "/_OK"): reusa; continue   # nunca chama processar()

A pasta do staging e indexada pelo NOME DO ARQUIVO, nao pela pasta de origem.
Mover o PDF de META_ANALISES para REVISOES nao mudava nada: o _OK continuava
la e a analise VELHA era republicada. A correcao manual do Dr. Eduardo ia para
o lixo num `continue` — que tambem pulava as tres travas que moram DENTRO de
processar(): a checagem de tipo no cache, o apagamento dos derivados e a LEI 8.

Segunda camada do mesmo buraco: _staging_atual(), escrita em 27/Jul contra
exatamente isto ("254 de 268 _OK eram de schema velho e furavam os consertos
por reuso"), estava DEFINIDA E NUNCA ERA CHAMADA. Codigo morto.

  1. rodar_em_blocos: _staging_serve() — so reusa se o tipo gravado nos fatos
     bater com a pasta de HOJE e o schema daquele tipo estiver presente.
     Staging sem `tipo_documento` = feito antes de 03/Ago = nao serve.
     Quando recusa, diz na tela POR QUE.
  2. rodar_em_blocos: PDF fora de pasta de tipo NAO ENTRA na fila (LEI 8) —
     antes virava 'original' calado, com motor e prompt no chute.
  3. rodar_em_blocos: codigo de saida passa a valer (1 se houve falha, 130 no
     Ctrl+C) + lista das falhas no fim, que antes rolavam a tela e sumiam.
  4. Chave 2: TRAVA DE SAIDA — o minirevisao.py so roda se o analisador
     terminar bem. Antes um Ctrl+C caia direto em mais 81 artigos pagos.
     Era o "eu interrompi e ele nao para".
  5. Chaves 1 e 2: mostram fila + custo estimado e pedem [s/N] antes de gastar.
     Eram as duas unicas que gastavam dinheiro sem perguntar nada.
  6. Chave 2: DIARIO em outputs/LOGS/chave2_*.log. So imprimia na janela, e
     quando a janela fechava a prova ia junto.
  7. teste_motor: teste_reuso_de_staging (16 combinacoes de pasta cruzada) e
     teste_pdf_sem_pasta_nao_entra — travas puras, sem LLM e sem rede.

Bateria: APROVADO. Todos os .py compilam.
