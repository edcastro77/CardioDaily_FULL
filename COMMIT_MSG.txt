Chave 8: prever o conflito add/add do llm_client (o portao pegou e abortou certo)

A 1a versao da Chave 8 previa 3 conflitos (CLAUDE.md, article_analyzer, radar_pubmed) e
ABORTOU ao encontrar um 4o: src/llm_client.py. O portao funcionou como devia - nao adivinhou
e nao deixou a pasta suja (merge --abort limpo).

POR QUE EU NAO TINHA PREVISTO: simulei a fusao com `git merge-tree` e procurei por
"changed in both". O llm_client.py e conflito ADD/ADD - as duas branches criaram o arquivo
de forma independente, a partir de um ancestral que nao o tinha. Add/add nao aparece como
"changed in both". Erro de analise meu, nao do script.

CONFERIDO FUNCAO A FUNCAO (01/Ago) - a LAB vence, com folga:
  main = 86 linhas, 5 funcoes
  lab  = 189 linhas, 9 funcoes - SUPERCONJUNTO ESTRITO
  A lab tem tudo o que a main tem, MAIS:
    - gerar_json (saida estruturada / tool use: JSON invalido vira impossivel)
    - _registrar_uso (instrumentacao: custo, cache, thinking, stop_reason)
    - contexto_uso (etapa/artigo no log)
    - _transitorio + retry com espera crescente
  gerar() - a funcao que o RADAR chama - tem ASSINATURA IDENTICA nas duas; a da lab so
  acrescenta retry em erro de rede. NADA que a main tenha falta na lab.

Tambem registrado nos comentarios da chave, para quem abrir amanha entender a decisao
sem precisar refazer a analise.
