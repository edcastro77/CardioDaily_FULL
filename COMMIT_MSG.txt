O PORTÃO DA CHAVE 8 RODAVA NO PYTHON ERRADO (01/Set/2026)

Ao mergear, o portão morreu com `ModuleNotFoundError: dotenv` no meio da
bateria — parecendo defeito do motor. Não era: o PASSO 2/4 chamava `python3`
puro, o do SISTEMA, que não tem as dependências do projeto. A prova de verdade
(91 travas) passa — foi provada hoje várias vezes no venv, inclusive em
ambiente vazio.

Conserto: o portão agora usa `$CD_VENV/bin/python` (o mesmo ambiente da
produção), com guarda que recusa se o venv não existir. Prova rodando no
python errado é prova de OUTRA coisa — e reprovar por ambiente parecendo
reprovar por conteúdo é exatamente o formato de confusão que a casa persegue.

(A propósito do estado do git: os dois commits de 01/Set já estavam na main —
a Chave 7 commitou na branch corrente — e a lab está idêntica a ela. Este
commit entra pela lab e sobe pela própria Chave 8, já consertada.)
