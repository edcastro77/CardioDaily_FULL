A CHAVE 8 TINHA PERDIDO A CHAVE DE SI MESMA (01/Set/2026)

Ao rodar o merge, o macOS recusou: "não possui os privilégios de acesso
apropriados". Medido: `8_Merge_para_Main.command` e `11_Prova_da_Extracao.command`
estavam `-rw-------` — sem o bit de execução que as outras 22 chaves têm
(`rwx--x--x`). Causa provável: algum editor regravou os dois arquivos e o
salvamento derrubou o bit.

Conserto: `chmod 711` nos dois, de volta ao padrão da pasta. O git rastreia o
modo do arquivo, então a correção viaja no repositório — se acontecer de novo
num clone, é só rodar o `chmod +x *.command` do LEIA-ME.

Vai junto o `.claude/settings.local.json` com as permissões acumuladas da
sessão de 01/Set (só allowlist de comandos; nenhum segredo).
